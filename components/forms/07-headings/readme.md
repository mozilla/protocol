There are two classes for styling headings that appear in forms:
`mzp-c-form-heading` and `mzp-c-form-subheading`

### Tips

- Use semantic headings to communicate the hierarchy of headings within the document.
- The subheading styles match the styling of the `<legend>` element.
